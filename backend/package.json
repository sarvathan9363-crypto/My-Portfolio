{
  "name": "portfolio-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "Sarvathan C — Portfolio Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "set NODE_OPTIONS=--dns-result-order=ipv4first && nodemon --env-file=.env server.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "nodemailer": "^6.9.8"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}